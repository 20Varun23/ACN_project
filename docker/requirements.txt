# Ryu 4.34 only works against this exact trio. Do not bump them: newer
# eventlet drops eventlet.wsgi.ALREADY_HANDLED (which ryu/app/wsgi.py imports)
# and newer dnspython changes the API eventlet's greendns expects.
ryu==4.34
eventlet==0.30.2
dnspython==1.16.0

# Analysis only (Stream C). Upper bounds keep them on Python 3.8 wheels.
pandas>=1.3,<2.1
matplotlib>=3.5,<3.8
